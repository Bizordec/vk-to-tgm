default_language_version:
  python: python3.14
repos:
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.16.2
    hooks:
      - id: commitizen
      - id: commitizen-branch
        stages: [pre-push]
  - repo: builtin
    hooks:
      - id: check-added-large-files
      - id: check-merge-conflict
      - id: check-toml
      - id: check-yaml
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: trailing-whitespace
  - repo: local
    hooks:
      - id: check-ast
        name: check python ast
        entry: python3 -m py_compile
        language: system
        types: [python]
      - id: lib-sync-locks
        name: Sync dependent project lock files on lib changes
        entry: bash -c 'for dir in projects/*; do (cd "$dir" && uv lock); done'
        language: system
        files: ^libs/[^/]+/(pyproject\.toml|uv\.lock)$
        pass_filenames: false
